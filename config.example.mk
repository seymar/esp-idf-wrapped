### Environment configuration

## Where to install common dependencies and tools with `make install` ?
## Installed dependencies and tools can be used by other projects to save space.
COMMON_DEPS_PATH := $(HOME)/dev/common

APP := hello
PORT ?= 
